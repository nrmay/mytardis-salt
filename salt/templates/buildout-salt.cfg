[buildout]
extends = buildout.cfg
eggs += psycopg2
eggs -= python-ldap

[django]
settings = settings

[uwsgi]
module = wsgi
chdir = {{ mytardis_dir }}
uid = mytardis
gid = mytardis
chown-socket = mytardis:www-data
chmod-socket = 660
